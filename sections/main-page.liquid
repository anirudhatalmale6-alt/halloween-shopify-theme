<section class="sec sec--top">
  <div class="wrap wrap--narrow">
    <h1 class="sec__h">{{ page.title }}</h1>
    <div class="rte page__body">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{
  "name": "Page"
}
{% endschema %}
